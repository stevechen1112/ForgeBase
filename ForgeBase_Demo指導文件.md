# ForgeBase Demo 指導文件
### 外銷製造商官網成長系統 — 客戶展示腳本

> **使用說明**：本文件依「展示流程」編排，每個段落包含展示操作步驟、展示重點與建議話術。
> 預估完整 Demo 時間：**75–100 分鐘**（可依客戶興趣裁剪）。
>
> 服務網址：
> - 前台官網 → `https://mitselect.com`
> - 管理後台 → `https://mitselect.com/backend`
> - API 文件 → `https://mitselect.com/api/docs`

---

## ⚙️ Demo 前置準備（必讀）

### Demo 的運作原理

**這個 Demo 不是靜態 UI 展示**。前台官網的每一次點擊、瀏覽都會即時寫入後台資料庫，管理後台即時呈現。換句話說：

> 你從前台扮演買家來瀏覽，後台就會出現對應的事件、分數變化、訪客記錄。

但「剛清空的後台」效果很差——訪客列表空白、RFQ 收件箱是零，完全無法說明系統在做什麼。正確的做法是：

```
[Demo 前] 跑 Seed 腳本 → 後台已有歷史軌跡（看起來系統已在運作）
[Demo 當下] 現場瀏覽前台 → 後台即時出現新事件（現場感）
```

---

### 步驟一：確認服務正常

服務已部署於雲端，無需本地啟動。直接瀏覽器確認以下三個網址可正常存取：

- 前台官網：`https://mitselect.com`
- 管理後台：`https://mitselect.com/backend`
- API 文件：`https://mitselect.com/api/docs`

---

### 步驟二：匯入內容資料（產品、應用等）

> **內容資料已匯入雲端，可略過此步驟。**

前台已有真實的產品、應用場景、認證等頁面內容可直接展示。

---

### 步驟三：注入模擬訪客行為（Demo 狀態準備）

```bash
cd /Users/yuchuchen/Desktop/ForgeBase
source .venv/bin/activate
python3 scripts/demo_seed_and_verify.py --mode demo \
  --email admin@forgebase.com --password ForgeBase2026
```

執行後後台會出現：

| 資料類型 | 數量 | 說明 |
|---|---|---|
| Tracking Events | ~1,400+ 筆 | 橫跨過去 30 天，模擬真實流量分布 |
| Unique Visitors | ~120 個 | 含 Hot / Warm / Cold 各比例 |
| Sessions | ~210 個 | 每個 session 3–9 個事件 |
| 聯絡表單 | 4 筆 | 各帶 visitor_id，可追溯來源 |
| RFQ 詢價單 | 6 筆 | 帶產品關聯、優先級自動判定 |

**後台帳號**：`admin@forgebase.com` / `ForgeBase2026`

---

### 步驟四：Demo 當下的「現場感」操作

Demo 期間最有說服力的時刻：**客戶親眼看到系統即時記錄行為**。

1. 打開一個**新的無痕視窗**（`⌘ + Shift + N`），開啟 `https://mitselect.com`
2. 瀏覽幾個產品頁、展開一個 FAQ、點擊 CTA 按鈕
3. 切回管理後台 → 追蹤 → 事件列表 → 畫面重新整理
4. **剛才的行為立即出現在後台**，並帶著 intent 分數變化

> 進階：同時開 2 個無痕視窗代表「2 位不同買家」，展示後台同時出現不同 visitor_id 的記錄。

---

---

## 行銷漏斗快速索引（Demo 規劃用）

> 如依**買家旅程**而非系統模組規劃展示順序，可用此表快速對應。

| 漏斗階段 | 買家狀態 | ForgeBase 機制 | Demo 章節 |
|---|---|---|---|
| **Awareness** | Google 搜尋關鍵字 | Page Brief + AI生成 + SEO稽核工具 | 1-3、1-4、1-5 |
| **Discovery** | 第一次進官網 | 前台頁型、產品/應用/比較頁 | 1-1 |
| **Engagement** | 細看、展開FAQ | First-party事件追蹤 | 2-1 |
| **Interest** | 回訪、下載規格書 | Download Gate、意圖評分、衰減 | 2-2、3-2 |
| **Consideration** | 高意圖互動 | Dynamic CTA（個人化）、ML評分 | 2-2、3-7 |
| **Retargeting** | 離站後廣告觸達 | 受眾同步（Google/Meta/LinkedIn） | 3-6 |
| **Intent Signal** | 業務情報 | 帳戶智能（哪家公司在看） | 2-4 |
| **Conversion** | 送出 RFQ | RFQ流程、業務Alert、CRM同步 | 3-1、3-3、3-4 |
| **Nurture** | 尚未轉換者 | Nurture序列 + ESP郵件 | 3-8 |
| **Optimize** | 持續改善 | 分析儀表板、SEO稽核、A/B Test | 第四部分 |

> 建議話術（漏斗開場白）：
> 「我用一個真實的工業買家旅程走一遍這個系統——從他在 Google 搜尋你的產品，到你的業務接到有意義的詢盤，這中間每一步 ForgeBase 都在做什麼。」

---

## Demo 開場（5 分鐘）

### 核心主張

> 「我們不是幫製造商做漂亮官網，而是把官網變成能**捕捉需求、辨識意圖、推進詢價**的實用型成長系統。」

### 三層架構說明圖（開場白用）

```
┌─────────────────────────────────────────────────────────┐
│  Capture Layer — 讓目標買家能搜尋到、理解、引用你的頁面   │
│  - 頁型模板  - AI 內容生成  - SEO 基礎設施               │
├─────────────────────────────────────────────────────────┤
│  Intent Layer — 辨識誰在看、看什麼、意圖有多強           │
│  - First-party 事件追蹤  - 意圖評分  - 受眾分類          │
├─────────────────────────────────────────────────────────┤
│  Conversion Layer — 把高意圖流量推進到詢盤與業務跟進      │
│  - RFQ 流程  - 再行銷受眾  - Nurture  - 業務 Alert       │
└─────────────────────────────────────────────────────────┘
```

**建議話術**：
> 「傳統官網三層都是空的——搜尋不到你、看不出誰有興趣、表單送出後業務也沒有行動依據。我們把這三層都填滿，而且全部原生在官網上，不需要再疊一堆第三方工具。」

---

## 第一部分：Capture Layer — 內容與頁面系統（20 分鐘）

### 1-1 前台官網整體瀏覽

**操作**：開啟 `https://mitselect.com`

**展示重點**：
- 首頁 Hero、產品分類卡片、應用場景摘要、認證展示
- **主推產品區塊**（「Featured Products」）— 當後台有任一產品被標記為主推時自動出現，優先展示高優先度產品
- 麵包屑（Breadcrumb）自動產生
- 導覽到產品分類頁 `/products/`
- 導覽到單一產品詳頁 `/products/{category}/{product}/`

**建議話術**：
> 「這是訪客看到的前台。每一頁都是依標準化頁型建出來的，不是自由排版，這樣 SEO 的 structured data、breadcrumb、internal link 才能自動運作。首頁的主推產品區塊是後台控制的——業務想把哪個產品推到第一眼，不需要改程式，在後台點一下星星就完成。」

---

### 1-2 管理後台 — 產品資料管理

**操作**：切換到 `https://mitselect.com/backend` → 登入 → 產品管理

**展示重點**：

1. **產品列表** — 可依分類、狀態（草稿/已發布/封存）篩選
2. **⭐ 主推產品切換** — 每筆產品旁有星星按鈕，點一下即標記為「主推」（金色星星），前台首頁的 Featured Products 區塊立即反映；再點一下取消。無需儲存按鈕，即時生效。
3. **新增或開啟一個產品**，展示欄位：
   - 產品名稱、型號（model_number）
   - 規格鍵值對編輯器（名稱 + 值 + 單位）
   - 圖片上傳（支援多張）
   - 富文字產品說明
   - 所屬分類選擇
4. **Entity 關聯面板** — 展示可直接在產品上設定：
   - 關聯應用場景
   - 關聯認證
   - 關聯 FAQ
   - 設定替代料號（雙向自動建立）

**建議話術**：
> 「產品資料不只是填名稱和規格。我們讓每個產品都能關聯到它適用的應用場景、認證、FAQ，這些關聯會自動產生頁面上的內連，也是 AI 生成文案時的知識來源。孤立的資料在這個系統裡沒辦法發揮作用。」

---

### 1-3 內容策略地圖（Page Brief 系統）

**操作**：管理後台 → 內容策略 → Page Brief 列表

**展示重點**：

1. **Page Brief 狀態流程**：
   ```
   草稿 → 審核通過 → 生成中 → 已完成 → 已發布
   ```
2. **開啟一個既有 Brief**，展示欄位：
   - 頁面類型（產品詳頁 / 應用頁 / FAQ 頁…）
   - 目標市場（美國、德國、日本…）
   - 目標客群（採購、工程師、代理商…）
   - 搜尋意圖（比較型、規格型、採購型…）
   - 主要關鍵字群
   - Primary CTA 指定

**建議話術**：
> 「在 AI 生成任何文案之前，我們強制要求先定義這一頁為什麼存在。目標市場是誰、要捕捉什麼搜尋意圖、這一頁的轉換目標是什麼。不允許先生成再期待流量，這樣的設計讓每一頁都有商業目的。」

---

### 1-4 AI 內容生成

**操作**：管理後台 → 找到一個 Approved 狀態的 Page Brief → 點擊「生成初稿」

**展示重點**：

1. AI 根據 Brief + 產品規格資料生成結構化初稿，包含：
   - SEO title（60 字上限，關鍵字置前）/ meta description（155 字，含行動短語）
   - 產品全文說明（依 Brief 目標字數，含 H2/H3 標題結構與關鍵字佈局）
   - FAQ 建議（3–5 個，問句格式符合 Google PAA 篩選規則）
   - CTA 主標題 / 副標題
   - **內連建議（internal_link_suggestions）**：AI 同步建議此頁應連結到哪些相關產品、分類或應用頁，附建議錨文字與理由

2. **每個欄位旁邊有狀態標記**：`AI 生成` / `已人工審核` / `人工撰寫`
3. **可逐欄修改**，也可「重新生成」整段

**建議話術**：
> 「AI 不是用來一鍵發布的，是用來降低內容建置成本的加速器。生成出來的是初稿，每個欄位都有審核狀態，強制人工確認後才能發布上線。」

> 「生成的文案從一開始就內建 SEO 品質標準——標題不超過 60 字、meta description 有行動短語、內文有清楚的 H2 結構、FAQ 問句格式符合 Google PAA 規則。這不是人工事後補救，是生成時就直接做對。」

**展示 AI 生成限制**：
> 「我們不提供自由 prompt——因為那樣生成的文案品質不穩定、跟業務目標脫節。這個系統的 AI 只在有 Brief 和結構化資料的基礎上運作。」

---

### 1-5 頁面發布與 SEO 基礎設施

**操作**：管理後台 → 頁面管理 → 選一個完成的頁面 → 發布

**展示重點**：

1. **發布後前台自動上線** — 訪問對應 URL
2. **SEO 自動輸出**，在瀏覽器開發工具 Sources / Elements 確認：
   - `<title>` 與 `<meta name="description">` 正確填入
   - `<link rel="canonical">` 自動生成
   - JSON-LD Structured Data（`Product` schema 或 `FAQPage` schema）
3. **取消發布（Unpublish）**：
   - 頁面狀態回到草稿
   - `noindex` 自動加回，搜尋引擎不會二次索引

**建議話術**：
> 「技術 SEO 是基礎設施，不應該是人工設定的。canonical、structured data、noindex 這些全部系統自動處理，內容人員只需要專注在文案品質上。」

---

### 1-6 預覽功能（草稿預覽）

**操作**：管理後台 → 找一個草稿狀態的頁面 → 點「產生預覽連結」

**展示重點**：
- 系統產生一個有時效性的預覽 Token（1 小時有效）
- 複製連結在前台開啟 → 顯示醒目的 `⚠ PREVIEW MODE` 警示橫幅
- Token 過期後再開啟 → 自動導向 404，不會洩漏草稿內容

**建議話術**：
> 「內容還在草稿時，客戶或主管想先看效果，可以用這個安全預覽連結，時效過了就失效，不用擔心沒完成的頁面被搜尋引擎抓到。」

---

### 1-7 SEO 稽核工具

**操作**：管理後台 → SEO 稽核

**展示重點**：

1. **總覽 Dashboard**：全站 SEO 健康分數
2. **On-Page 稽核**：逐頁列出缺少 meta description、title 過長／過短、h1 缺失等問題
3. **關鍵字蠶食偵測（Cannibalization）**：自動找出多個頁面搶同一關鍵字的問題，建議整并或差異化方向
4. **機會列表（Opportunities）**：依流量潛力排列尚未最佳化的頁面，並提供可執行的建議
5. **一鍵最佳化（AI Optimize）**：對選定頁面執行 AI 改善（title、meta description、內文建議）

**建議話術**：
> 「大部分製造商的官網有幾十個產品頁，手動管理 SEO 根本做不到。這個 SEO 稽核工具會主動告訴你哪些頁面有問題、哪些頁面有機會，而且你自己的頁面在搶同一個關鍵字這種蠶食問題，人工根本找不到，系統自動監控。」

---

## 第二部分：Intent Layer — 訪客意圖追蹤（18 分鐘）

### 2-1 First-Party 事件追蹤

**操作**：開啟前台任一頁面，同時在管理後台 → 追蹤 → 事件列表 → 即時重新整理

**展示重點**：

展示以下事件自動被記錄（不需要 GA）：

| 操作 | 自動觸發的事件 |
|------|---------------|
| 進入首頁 | `page_view` |
| 進入產品分類頁 | `category_view` |
| 點擊進入產品詳頁 | `product_view`（附帶 product_id） |
| 點擊展開 FAQ | `faq_expand` |
| 點擊下載規格書 | `spec_download` |
| 點擊 CTA 按鈕 | `cta_click` |
| 開始填寫 RFQ | `rfq_start` |

**建議話術**：
> 「這是我們自己的 first-party 事件，不是 Google Analytics 的 session。這些事件帶著 visitor_id、product_id、page_type，讓我們能算出每個訪客的商業意圖分數，而不只是看頁面停留時間。」

---

### 2-2 意圖評分系統（Intent Scoring）

**操作**：管理後台 → 追蹤 → 訪客列表（依分數排序）

**展示重點**：

評分規則即時展示：

| 行為 | 分數 |
|------|------|
| 瀏覽產品詳頁 | +3（重複瀏覽 +2） |
| 瀏覽應用頁 | +4 |
| 展開 FAQ | +6（展開 3+ 個 +4） |
| 查看比較頁 | +6 |
| 下載規格書 | +8 |
| 點 Primary CTA | +8 |
| 開始填 RFQ | +15 |
| **送出 RFQ** | **+30** |
| 7 天內回訪 | +6 |

**意圖階段（Intent Stage）**：

| 分數 | 階段 | 說明 |
|------|------|------|
| 0–9 | ❄️ Cold | 只是瀏覽 |
| 10–29 | 🟡 Warm | 有互動行為，進入再行銷 |
| 30–59 | 🔥 Hot | 明確產品興趣，觸發業務 Alert |
| 60+ | ✅ Sales-Ready | 直接跟進，通知業務優先處理 |

**展示衰減機制**：
> 「分數不是只增不減。7 天沒有活動，分數乘以 0.8；14 天乘以 0.5；30 天乘以 0.2；60 天歸零。這樣業務名單永遠是最新鮮的。」

---

### 2-3 訪客詳頁（Visitor Timeline）

**操作**：管理後台 → 追蹤 → 點進一個 Hot 或 Sales-Ready 的訪客

**展示重點**：
- 事件時間軸：看他這幾次 session 都看了哪些產品、哪些應用、有沒有展開 FAQ
- 當前 intent_score 與 intent_stage
- 關聯的 Contact（若曾填過表單）
- 來源頁 source_page 記錄（知道他是從哪個行銷活動或搜尋關鍵字進來）

**建議話術**：
> 「業務不需要靠直覺猜客戶要什麼。這個時間軸直接告訴你：這個人看了哪個產品分類、在哪個應用頁停留最久、下載過規格書。業務跟進時就有具體的談話切入點。」

---

### 2-4 帳戶智能（Account Intelligence）⭐ B2B 核心賣點

**操作**：管理後台 → 帳戶 → 帳戶列表（依活動日期排序）

**展示重點**：

1. **公司層級追蹤**：系統不只追蹤「一個訪客」，而是追蹤「一家公司」的多個瀏覽者行為加總
2. **IP 反查公司**：對未填資料的匿名訪客，系統可依 IP 位置推測公司名稱與產業
3. **帳戶 Insight 面板**（點進一個帳戶）：
   - 該公司共有幾位不同訪客造訪
   - 累計瀏覽了哪些產品類別
   - 最高意圖訪客的 intent_score
   - 是否已有關聯的 Contact 或 RFQ
4. **帳戶層級 Enrichment**：可呼叫外部資料庫補充公司規模、產業、所在地

**建議話術**：
> 「工業 B2B 的採購決策不是一個人做的。一家德國汽車零件廠可能有採購、工程師、主管三個人分別在不同時間看你的頁面——我們能把這些軌跡歸到同一個帳戶。業務不只能看到訪客，還能看到『哪家公司』這個月反覆造訪、還沒詢價。這就是業務主動出擊的依據。」

---

## 第三部分：Conversion Layer — 詢盤閉環（25 分鐘）

### 3-1 RFQ 詢價前台流程

**操作**：開啟前台 `https://mitselect.com/request-quote` 或從產品頁點擊詢價 CTA

**展示重點**：

1. **從產品頁進入 RFQ**：產品欄位自動帶入（無需重複選擇）
2. **表單欄位**：姓名、公司、Email、國家、職稱、感興趣產品、應用場景、數量、規格需求、採購時程
3. **同意聲明**（必填）
4. **表單草稿自動儲存**：中途關閉瀏覽器，重新打開草稿還在
5. **送出後確認頁**：顯示 RFQ 編號（`RFQ-20260315-001`）

**建議話術**：
> 「從產品頁直接點詢價，產品欄位自動帶入，降低填表摩擦。表單會記住草稿，買家不怕中途打斷。送出後業務端馬上會收到通知。」

---

### 3-2 下載門欄（Download Gate）

**操作**：前台產品頁 → 找到規格書下載按鈕 → 點擊

**展示重點**：
1. 跳出 Modal 要求留下基本聯絡資料（姓名、Email、公司）
2. 送出後直接提供下載連結
3. 後台自動建立/更新 Contact 記錄
4. `spec_download` 事件同步寫入，intent 分數 +8

**建議話術**：
> 「下載規格書是高意圖行為，我們把它變成一個留資機會。填完資料才能下載，而且系統自動記下他下載的是哪個產品的文件，這個訊號直接反映在意圖分數上。」

---

### 3-3 管理後台 — RFQ 收件箱

**操作**：管理後台 → 轉換 → RFQ 列表

**展示重點**：

1. **RFQ 狀態追蹤**：
   ```
   新建 → 已指派 → 處理中 → 已報價 → 成交 / 失單 / 過期
   ```
2. **指派業務**：點擊 RFQ → 指派給對應業務人員
3. **優先度標記**：意圖分數 ≥ 30 的 RFQ 自動標記高優先（🔥）
4. **開啟 RFQ 詳頁**，展示完整資訊：
   - 聯絡人資料 + 公司
   - 感興趣的產品列表
   - 應用場景
   - 採購時程
   - 送出時的意圖分數（`intent_score: 42, stage: Hot`）
   - 來源頁（`source_page: /products/hydraulic-seals/model-x100/?ref=product`）
   - **業務追蹤欄位**：`第一回應時間`（first_response_at）、`報價送出時間`（quote_sent_at）、`失單原因`（lost_reason）— 業務可在詳頁直接填記，不需要另開 CRM

**建議話術**：
> 「業務打開一個 RFQ，不只看到表單資料，還能看到這個人的意圖分數是 42（Hot 階段），而且他是從哪個產品頁點過來的。跟進時完全不是盲猜。報價送出了就記一下時間，失單了就留一個原因——這些資料最後會反映在轉換儀表板的平均回應時長和失單原因分析上。」

---

### 3-4 自動通知機制

**展示重點（說明流程，不一定要現場觸發）**：

| 觸發條件 | 通知方式 | 接收者 |
|----------|----------|--------|
| 新 RFQ 建立 | Email + 系統內通知 | 被指派業務 |
| RFQ 意圖分數 ≥ 30 | 高優先 Email（特別標記） | 業務 + Admin |
| RFQ 超過 24h 未處理 | Email 提醒 | 業務 |
| RFQ 超過 48h 未處理 | Email 升級 | Admin |
| 訪客進入 Hot 階段 | 系統 Alert | 業務 |

---

### 3-5 Contact 聯絡人管理與去重

**操作**：管理後台 → 追蹤 → 聯絡人列表 → 點開一個聯絡人

**展示重點**：
- 同一 Email 多次送表單 → 自動去重（更新，不新增）
- 聯絡人詳頁顯示：
  - 歷史提交的所有 RFQ 與聯絡表單
  - 來源頁紀錄（`source_page` 含完整 query string）
  - 當前 intent_score 與 intent_stage
  - 下載過的文件列表

---

### 3-6 再行銷受眾同步（Ads Audience）

**操作**：管理後台 → 整合 → 受眾同步

**展示受眾類型**：

| 受眾 | 同步條件 | 平台 |
|------|----------|------|
| High-Intent Visitors | `intent_stage = Hot / Sales-Ready` + 有 Email | Google Ads Customer Match |
| Product Interest | 瀏覽特定分類 ≥ 3 頁 + 有 Email | Google Ads |
| RFQ Submitted | 已送出 RFQ | Meta Custom Audience |
| Return Visitors | `return_visit` 事件 + 有 Email | Meta / Google |
| LinkedIn Account List | 高意圖公司名單 | LinkedIn Matched Audiences（Phase 2） |

**Meta Conversions API 事件映射**：

| 系統事件 | → Meta 事件 |
|----------|-------------|
| `product_view` | ViewContent |
| `rfq_start` | InitiateCheckout |
| `rfq_submit` | Lead |
| `spec_download` | AddToCart |

**建議話術**：
> 「工業 B2B 買家極少第一次來就詢價。我們把高意圖訪客自動丟進廣告再行銷名單，讓 Google / Meta 在他們下次上網時持續出現。這個名單不是人工維護的，是系統根據意圖分數每日自動更新。」

---

### 3-7 Dynamic CTA（個人化行動呼籲）

**操作**：管理後台 → CTAs → 檢視 CTA 列表 → 開啟任一 CTA 編輯

**展示重點**：

系統根據訪客當前的意圖階段，在同一個頁面展示不同的 CTA：

| 訪客狀態 | 看到的 CTA | 邏輯 |
|---|---|---|
| Cold（第一次造訪） | 「下載規格書」 | 低門檻留資，先捕捉信號 |
| Warm（已有互動記錄） | 「查看完整規格比較」 | 引導深度探索 |
| Hot（高分訪客） | 「立即詢價」+ 表單前置帶入 | 縮短轉換路徑 |
| Sales-Ready | 「預約工程師諮詢」 | 升級接觸層級 |

**後台展示重點**（v0.18 新增）：
- CTA 編輯表單中的「**目標意圖階段**」欄位（`target_intent_stage`）— 下拉選單可設定 `any / cold / warm / hot / sales_ready`
- 建立兩個同 `cta_key` 的 CTA，一個設 `cold`、一個設 `hot`，前台會依訪客分數自動選擇顯示哪一個
- 展示步驟：後台建好規則 → 前台刷新頁面 → 意圖不同的訪客看到不同的 CTA 文案

**建議話術**：
> 「傳統靜態網頁所有人看到一樣的 CTA。ForgeBase 的 Dynamic CTA 會根據這個訪客之前的行為——他已經下載過規格書了、他看了三個產品分類——給他看最符合他當下需求的行動呼籲。你在後台設定每個 CTA 鎖定哪個意圖階段，不需要改程式，行銷人員自己就能操作。」

---

### 3-8 Nurture Email 序列（培育尚未轉換的聯絡人）

**操作**：管理後台 → Nurture → 序列列表 → 點「新增序列」或開啟現有序列

**展示重點**：

1. **序列設定**（`/backend/dashboard/nurture/new` 或 `/backend/dashboard/nurture/[id]`）：
   - 序列名稱（例如「液壓密封件冷啜」）
   - 觸發條件：`intent_stage = Warm` + 14 天未提交 RFQ
   - 步驟列表：每個步驟含延遲天數、郵件主旨、正文
   - 步驟可動態新增/刪除/排序
2. **ESP 整合**（管理後台 → ESP 狀態）：
   - 連結 Mailchimp 或 SendGrid 帳號
   - 同步聯絡人至對應 List
   - 查看發送統計（開信率、點擊率）
3. **意圖回流**：Contact 在 Nurture 郵件中點擊連結回到官網 → 事件重新觸發 → 分數繼續累積

**建議話術**：
> 「填了下載表單但沒有詢價的聯絡人，不是該放棄的。他進入培育序列，系統在接下來的幾週發幾封針對他感興趣的產品的說明信。他只要點進來繼續瀏覽，意圖分數就繼續往上跑，下次進入 Hot 就會再度觸發業務 Alert。」

---

## 第四部分：分析儀表板（10 分鐘）

### 4-1 內容成效儀表板

**操作**：管理後台 → 分析 → 內容成效

**展示重點**：

| 指標 | 說明 |
|------|------|
| 各頁面的 page_view 數 | 哪些產品頁流量最高 |
| 各頁面的高意圖互動率 | 哪些頁面帶來高分訪客 |
| spec_download 次數 | 哪些規格書最受歡迎 |
| cta_click → rfq_submit 這條路徑的轉換率 | 哪個頁面的 CTA 效果最好 |

---

### 4-2 漏斗分析（Funnel Analytics）⭐ v0.18 新增

**操作**：管理後台 → 分析 → 漏斗（`/backend/dashboard/analytics/funnel`）

**展示重點**：
- 完整漏斗視覺化：`到達 → 有互動 → 下載/高意圖 → 開始 RFQ → 送出 RFQ`
- 每個漏斗層的人數與轉換率
- 最大流失點一眼看出（例如「有互動但沒有下載規格書」這層流失 73%）
- 可作為改善切入點的依據：流失最多的那層，優先加強 CTA 或降低門檻

**建議話術**：
> 「這個漏斗圖直接告訴你問題在哪裡。如果大量訪客在『有互動但未下載』那層流失，代表規格書的入口 CTA 需要調整，或者下載門檻太高。這樣的決策依據是數據，不是感覺。」

---

### 4-3 Intent 儀表板

**操作**：管理後台 → 分析 → 意圖總覽

**展示重點**：
- 依 intent_stage 的訪客分佈漏斗：Cold → Warm → Hot → Sales-Ready
- 今日新增 Hot 訪客數
- 高意圖訪客最感興趣的產品分類 Top 5
- 訪客從 Cold → Hot 的平均天數

---

### 4-4 RFQ 轉換儀表板

**操作**：管理後台 → 分析 → 轉換

**展示重點**：
- RFQ 數量趨勢（週 / 月）
- RFQ 來源分佈（哪個產品頁、哪個廣告活動帶來最多詢盤）
- RFQ 各狀態數量（新建 / 指派 / 已報價 / 成交）
- 平均從 RFQ 建立到第一個業務回應的時間

---

## 第五部分：整合能力（5 分鐘說明）

### 5-1 GA4 並行追蹤

**建議話術**：
> 「所有 ForgeBase 的 first-party 事件，也會同步對應地發送到 GA4，例如 `product_view → view_item`、`rfq_submit → generate_lead`。GA4 是輔助報表用，但業務邏輯和意圖評分的核心跑在我們自己的資料上，不依賴 GA4。」

### 5-2 CRM 同步（HubSpot 為例）

**展示重點**：
- RFQ 送出 → 自動建立/更新 HubSpot Contact + Deal
- Contact 欄位映射：姓名、Email、公司、國家、職稱
- Deal 含 intent_score、products_of_interest、source_page 等自訂欄位
- 同步方向：Phase 1 為系統 → CRM 單向

### 5-3 Webhook

**展示重點**：

支援的 Webhook 事件（即時觸發）：

| 事件 | 觸發時機 |
|------|----------|
| `rfq.created` | 新 RFQ 建立 |
| `rfq.status_changed` | RFQ 狀態變更 |
| `contact.created` | 新聯絡人 |
| `contact.intent_stage_changed` | 意圖階段改變 |
| `visitor.became_hot` | 訪客進入 Hot 階段 |

Webhook payload 含 HMAC-SHA256 簽章，失敗自動重試三次。

---

## 第六部分：客制與延伸討論（視需求）

### 可展示但視客戶需求決定的功能

| 功能 | 說明 | 對應客群 |
|------|------|----------|
| 多語管理（Phase 2） | 英文 / 中文 / 德文 / 日文各自維護 | 有多語市場的製造商 |
| A/B Test 框架 | CTA 文案 / 頁面版本測試 | 已有一定流量的客戶 |
| Nurture Email 自動化（Phase 2） | 依意圖階段觸發 email 序列 | 有 email 名單的客戶 |
| AI RFQ 分析與回覆草稿（Phase 3） | AI 分析 RFQ 內容，協助業務擬定回覆 | 業務量大的客戶 |
| Salesforce 整合（Phase 2+） | 雙向 CRM 同步 | 已用 SF 的大型製造商 |
| LinkedIn Matched Audiences | 公司帳戶級再行銷 | 高客單價 B2B |

---

## Demo 常見 Q&A

**Q：這和一般 CMS（WordPress / Webflow）有什麼不同？**
> A：一般 CMS 只管前台頁面，沒有意圖追蹤、沒有 RFQ 閉環、沒有再行銷受眾同步。ForgeBase 是把 Capture、Intent、Conversion 三層整合在一個系統裡，官網本身就是業務工具，不只是型錄。

**Q：AI 生成的內容品質可靠嗎？**
> A：AI 生成的是「基於規格資料的結構化初稿」，不是憑空捏造的文案。每個欄位有審核狀態標記，未經人工確認不能發布。這個設計確保品質底線，同時把建置時間縮短 60–70%。

**Q：我們不會 SEO，系統能自己處理嗎？**
> A：技術 SEO 部分（canonical、structured data、sitemap、robots、breadcrumb）全自動。內容 SEO 部分（關鍵字、意圖對齊）由 Page Brief 系統引導操作，不需要有 SEO 背景。

**Q：系統收到的詢盤資料安全嗎？**
> A：所有資料存在你們自己的資料庫，不走任何第三方平台。Webhook 有 HMAC 簽章。訪客識別用 first-party cookie，不依賴第三方 cookie。

**Q：可以先從局部導入嗎？**
> A：可以。支援三種導入模式：
> 1. **標準導入**：全站一次上線
> 2. **過渡導入**：用子網域先接管 RFQ / 產品頁
> 3. **嵌入式導入**：在現有網站插入表單、追蹤元件，先驗證成效

---

## Demo 結尾建議話術

> 「我們今天看的是一個完整的官網成長系統，從頁面上線、搜尋曝光、訪客意圖辨識，到詢價閉環和再行銷。每一層都是工業 B2B 最需要、但傳統官網最缺的能力。
>
> 下一步可以用你們現有的產品資料跑一個 Pilot——我們建幾個產品頁、一個應用頁、把詢價流程接上來，你們就能看到實際效果，不需要等全站上線。」

---

*文件版本：v0.18 / 2026-03-15 | 對應系統：ForgeBase Phase 1a + 1b + v0.18 growth site 增強（已上線功能）*
