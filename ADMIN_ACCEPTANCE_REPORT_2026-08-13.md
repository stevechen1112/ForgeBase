# ForgeBase Admin 後台驗收報告

- 驗收日期：2026-08-13
- 驗收環境：本機 Admin `http://localhost:3001/backend`、FastAPI `http://127.0.0.1:8001`、PostgreSQL demo 資料庫
- 驗收範圍：一般後台、平台管理後台、四種租戶角色、超級管理員、內容 CRUD 契約、響應式操作、API 完整資料庫回歸
- 結論：**條件式通過。內部後台功能與權限可簽核；真實付款、寄信與第三方訊息仍須使用各供應商 sandbox／測試帳號完成最後整合簽核。**

## 驗收結果摘要

| 範圍 | 結果 | 驗證證據 |
|---|---|---|
| 後台主選單 | 通過 | 33 條一般後台主路由逐頁實機開啟，均有正確標題與可操作內容，無 404。 |
| 新增流程 | 通過 | 11 條獨立新增路由逐頁載入表單；RFQ 回覆範本為同頁新增，合計 12 種新增入口。 |
| 平台管理 | 通過 | 超級管理員登入、平台總覽、租戶、用戶、系統健康 4 條主路由均通過。 |
| 角色矩陣 | 通過 | owner、admin、marketing_manager、sales 與 super-admin 實際登入；側欄可見性與直接輸入受限網址的 403 均符合規則。 |
| 行動版 | 通過 | 390×844 驗證一般後台及平台後台選單開啟、導覽後自動收合，內容無橫向溢位。 |
| 商品流程 | 通過 | 清單、搜尋、語言篩選、分頁、新增表單與必填驗證通過；第 2 頁實測 13 筆。 |
| RFQ 流程 | 通過 | 全部／我的／範本／詳情、狀態、跟進與指派入口可用；指派改為有效團隊成員選單。 |
| 內容與成長工具 | 通過 | 分類、頁面、素材、應用、FAQ、認證、廠能、比較、CTA、轉址、意圖、對話、分群、nurture、通知等主路徑均通過。 |
| Admin lint/build | 通過 | production build 成功，共 58 條 Next.js route；lint 無 error，僅公開首頁 2 個既有 `<img>` 最佳化警告。 |
| API 無資料庫測試 | 通過 | `83 passed, 52 skipped`；跳過項為需 DB 的整合案例。 |
| API 完整資料庫回歸 | 通過 | `133 passed, 2 skipped`；2 項為未安裝可選 AgentOS 套件的 `importorskip` 案例。 |
| 外部副作用 | 待 sandbox | PayPal、Email/ESP、Telegram、LINE、HubSpot、Google Ads 未向真實服務送出資料。 |

完整逐路由與操作矩陣見 `ADMIN_UAT_MATRIX_2026-08-13.md`。

## 本次已修復與補強

1. 商品分類改為必填選單，排除封存分類，不再要求輸入 UUID；空白送出會得到瀏覽器可理解的必填提示。
2. API 驗證錯誤改為人類可讀文字，不再顯示 `[object Object]`。
3. 修正產品分頁被右下角 AI 助理浮鈕遮住的問題，並補上按鈕 `type="button"` 與中文頁碼。
4. 不支援語系（例如 `ja`）的內容列表改回傳空結果，不再靜默混入英文內容。
5. 權限從「只隱藏側欄」補強為直接網址也會顯示清楚的 403；涵蓋 admin-only 與 sales-hidden 路徑。
6. 一般後台及平台管理新增手機版選單、無障礙名稱與導覽後自動收合。
7. RFQ 指派改為有效團隊成員選單，顯示姓名、Email 或停用狀態；移除重複且工程導向的 `run_id` 查詢卡。
8. 儀表板的「近 30 天」摘要、逾期數與最新 RFQ 改用一致的 30 天範圍，不再把更早資料混入。
9. 移除舊的唯讀整合設定頁，舊網址永久導向唯一的可編輯整合設定入口。
10. 登入頁移除未經證實的成長數字、ISO/GDPR 宣稱，保留可驗證的安全登入描述。
11. AgentOS API 錯誤補上 HTTP 與回應摘要，方便營運人員判斷服務狀態。

## 安全邊界與未代替 sandbox 驗收的項目

下列操作可能寄出郵件、發送外部訊息、建立廣告／CRM 資料或改變訂閱，因此本次只驗證畫面、驗證規則、權限、API 契約與前置條件，沒有對真實服務執行最終送出：

- PayPal 升級、取消與付款核准回跳。
- Email/ESP 實際寄送與 nurture outbox 最終送出。
- Telegram、LINE webhook 實際通知。
- HubSpot、Google Ads 同步與寫入。

這些項目要取得「無條件通過」，需要提供隔離的 sandbox 帳號、測試收件人與可清除的測試資料。

## 清理紀錄

- CRUD 驗收建立的暫存商品與分類已於先前測試後依精確 ID 清除。
- 本次角色驗收使用 4 個 `uat-*` 暫存帳號；驗收完成後依精確 Email 刪除。
- 未刪除或覆寫任何既有商品、RFQ、聊天、通知、nurture 排程或第三方憑證。
