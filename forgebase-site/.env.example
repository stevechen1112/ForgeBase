# Production stays on same-origin paths when these values are left blank.
# Local preview defaults are http://localhost:3011 and the current Linode reference site.
NEXT_PUBLIC_TEMPLATE_BASE_URL=
NEXT_PUBLIC_NORTHFORGE_URL=
NEXT_PUBLIC_BACKEND_BASE_URL=
