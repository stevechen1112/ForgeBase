# ForgeBase AI 產品顧問：決策紀錄與施工規格

- 文件日期：2026-08-18
- 文件性質：產品決策紀錄＋實作規格；後續施工以此為準
- 配套方案：`FORGEBASE_AI_ADVISOR_RELIABILITY_AND_KNOWLEDGE_PLAN_2026-08-18.md`
- 衝突時：本文件的決策表優先；品質門檻與管線細節以方案文件為準

---

## 1. 這份文件在記什麼

2026-08-18 確認並鎖定公開 AI 客服的產品邊界、知識來源、後台上傳方式，以及與內部企業知識庫的差異。實作必須符合本文件，不得自行改回「第二階段才開」或做成員工內部知識庫。

---

## 2. 已鎖定的產品決策

| # | 決策 | 含義 |
|---|---|---|
| D1 | AI 客服是**第一階段預設功能** | 每個受管交付網站預設開啟 `ai_advisor` 與 `chat_handoff`。不是加購、不是進階測試、不是第二階段才有。 |
| D2 | 角色是公開接待，不是報價業務 | 依已確認公開資料說明產品、協助找品項、補齊詢價條件、交給真人。不報價、不保證交期、不對認證適用性下最終結論。 |
| D3 | 知識預設＝網站已發布內容 | 產品、分類、應用、能力、證書、FAQ、已發布非法律頁。內容人員不必另養一套「只給 AI 看」的庫。 |
| D4 | 後台可額外上傳文件當客服知識 | 位置是既有「圖片與檔案」。上傳後須勾選「允許公開索引」才進入公開客服。 |
| D5 | 可收文字 PDF 與 DOCX | 第一版收抽得出文字的 PDF／DOCX。不做掃描件 OCR 當正式來源。 |
| D6 | 預設不勾公開索引 | 同一素材庫同時服務網站下載與客服發言。檔在庫裡 ≠ 允許對陌生買主口述。 |
| D7 | 與產品頁衝突時，CMS 已發布欄位為準 | 檔案只能補充頁上沒寫全的細節。改正式規格必須改產品並發布。 |
| D8 | 出處分三層 | 系統核對要精確到檔與頁／段落；後台業務看得到檔名頁碼；前台買主只看到答案，以及他實際能打開的產品頁或下載連結。 |
| D9 | 取消勾選或下架，下一題就不能再用 | 切塊列立即不可見。這與用向量或關鍵詞無關；兩者都讀同一批列。 |
| D10 | 檢索是混合的 | 關鍵詞（型號、證書名、料號）＋語意／模糊用途＋當頁加權。 |
| D11 | 提示詞不開放客戶改 | 可改的是內容、開場白、建議問題、是否例外關閉。 |
| D12 | 例外才能關閉 | 系統方可關（內容空白、濫用、客戶書面要求）。預設是開。 |
| D13 | 刻意不納入公開客服 | 內部 SOP、草稿、底價、客戶名單、未公開產能、舊站匯入、AI 自動產文、訪客改 prompt、語音即時對話、買主詢價附件。 |
| D14 | 回答語言跟隨訪客，不綁網站內容語系 | 網站可只有英／繁中已發布內容，但訪客以日文、德文、韓文或其他語言提問時，顧問以同一語言解釋已核准資料；型號、數值與 grounding 邊界不因翻譯改變。 |

舊切法作廢：不得再把公開 AI 客服標成第二階段或「進階測試才開」。意圖評分、公司辨識、漏斗、培育郵件仍屬第二階段。

---

## 3. 為什麼和企業內部知識庫不同

內部知識庫服務已登入員工，目標是把公司檔案找出來。公開客服服務官網上的陌生買主，每一句都可能被當成工廠正式說法。

因此：

- 沒明示「允許公開索引」的檔，客服不能拿來回答。
- 抽不出文字就不假裝讀過。
- 買主畫面不強迫看檔名頁碼；精確出處留給核對與後台。
- 下架必須立即生效，不能等晚上重建索引。

---

## 4. 後台上傳長什麼樣子

### 4.1 現況（實作前）

- Enclave 有獨立「上傳進知識庫」路徑，檔案進庫即供內部問答。
- ForgeBase 已有 `/dashboard/assets`，可上傳圖片／PDF 供網站使用。
- `content_assets.is_indexable` 欄位存在，後台沒有開關，也沒有解析／檢索接到 Chat。
- 因此：**現在上傳 ≠ AI 客服知識。**

### 4.2 目標（實作後）

同一頁「圖片與檔案」：

1. 上傳 PDF 或 DOCX（以及既有圖片等網站素材）。
2. 對可抽字的文件顯示「允許公開索引」。
3. 勾選後編譯進公開知識切塊；取消或刪除後切塊立即不可見。
4. 列表顯示索引狀態：未索引／已索引／抽字失敗（`needs_ocr`）／已撤回。

網站 CMS 發布仍會自動入庫，不必把產品再傳一次檔。

---

## 5. 目標管線（實作必須落地）

```text
CMS 發布／檔案勾選公開
  → 耐久同步工作
  → 編譯切塊（公開、已發布、分語系）
  → 關鍵詞索引＋可選向量欄位
訪客提問
  → 租戶／功能／限流
  → 查詢規劃（找品項／問事實／高風險／注入）
  → 混合檢索（當頁強制納入）
  → 資料與使用者問題以「不可當指令」包進 prompt
  → 模型起草
  → 論點／數字必須出現在檢索原文，否則改為未確認
  → 一次追問一個缺口或轉 RFQ
  → 前台：答案＋買主可打開的連結＋狀態
  → 後台：來源、檔名頁碼、溯源結果
```

生產 API 目前為 1 個 uvicorn worker。限流仍改為資料庫列計數，避免日後加 worker 失效。

第一版向量：以 PostgreSQL 全文檢索為主路徑；embedding 欄位預留，有模型金鑰時可寫入，檢索時先用全文與關鍵詞召回再加權。不更換正式 Postgres 映像、不把 pgvector 當成上線阻擋項。掃描 OCR、獨立向量資料庫、語音，都不在本輪。

---

## 6. 資料表

`knowledge_sources`

- tenant_id、source_type、source_id、locale
- visibility 固定公開客服只讀 `public`
- status：`pending` / `indexed` / `stale` / `failed` / `tombstoned`
- title、canonical_url、content_hash、index_error、page_count

`knowledge_chunks`

- source 外鍵、tenant_id、chunk_index、page_number
- text、tsv（全文）、embedding_json（可空）
- metadata_json（型號、證書名）

`knowledge_sync_jobs`

- tenant_id、source_type、source_id、dedupe_key
- status：queued / running / succeeded / failed
- 由排程每分鐘領取，不用 fire-and-forget task 當唯一入庫

`chat_sessions` 增加 `qualification_json`（已填 slot）。

---

## 7. 驗收（本輪做完必須能證明）

1. 第一階段租戶預設 `ai_advisor`、`chat_handoff` 為開。
2. 發布產品後，首頁提問能檢索到該產品切塊，不必人先點進產品頁。
3. 未發布、他租戶、法律頁、未勾選檔案不得被檢索。
4. 勾選 PDF／DOCX 且抽得出字 → 客服可用；取消勾選後下一題不可用。
5. 掃描件或抽字失敗 → `needs_ocr`，客服當它不存在。
6. 回答中的規格數字若未出現在檢索原文，不得標 `grounded`。
7. 使用者問題與切塊在 prompt 裡被標成不可執行資料。
8. 注入類問題不洩漏系統規則。
9. 價格／交期保證／無證書的合規結論降級為未確認並導向 RFQ。
10. 後台素材列看得到公開索引開關與狀態。
11. 自動化測試覆蓋：編譯、檢索隔離、下架立即失效、數字溯源、功能預設。

---

## 8. 明確不在本輪做

- 更換生產資料庫為 pgvector 映像
- 掃描 OCR
- 語音客服
- 內部 SOP／底價知識層
- 租戶自訂系統提示詞
- 獨立知識庫產品或外接另一套問答服務

---

## 9. 本輪已落地的程式

| 區塊 | 位置 |
|---|---|
| 知識表／限流表／對話 slot | migration `0076`、`app/models/knowledge.py` |
| CMS／檔案編譯與立即下架 | `app/services/knowledge_compile.py`、`knowledge_sync.py` |
| 混合檢索（詞彙＋PostgreSQL FTS） | `app/services/knowledge_retrieve.py` |
| PDF／DOCX 抽字 | `app/services/knowledge_extract.py`（`pypdf`、`python-docx`） |
| Chat 全站檢索、prompt 隔離、數字溯源 | `chat_service.py`、`chat_grounding.py` |
| 共享限流 | `rate_limit.check_shared` + `rate_limit_hits` |
| 後台上傳開關與狀態 | `admin/.../dashboard/assets`；後台對話顯示檔名頁碼 |
| 評測骨架 | `app/services/knowledge_eval.py`、`tests/test_knowledge_advisor.py` |

上線前須對正式／測試資料庫執行 `alembic upgrade head`（至 `0076`），並安裝 API 新增依賴。

---

## 10. 修訂

| 日期 | 內容 |
|---|---|
| 2026-08-18 | 初版。收錄當日全部產品問答與實作規格，作為施工依據。 |
| 2026-08-18 | 接續完成階段 0–4 程式落地，並將實作位置寫入本節。 |
