import type { Metadata } from "next";
import { ElectronicsRFQPage } from "@/templates/electronic-components/components/ElectronicSite";

export const metadata: Metadata = { title: "Request Component Samples | Veltrix Demo", description: "Try a safe static sample-request workflow that creates no sample, lead, contact, order or email." };
export default ElectronicsRFQPage;
