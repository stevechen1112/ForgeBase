import type { Metadata } from "next";
import { AutomationContactPage } from "@/templates/industrial-automation/components/AutomationSite";

export const metadata: Metadata = { title: "Automation Diagnostic | Kinetra Demo", description: "Build a local-only automation consultation brief from a line constraint and integration boundary." };
export default AutomationContactPage;
