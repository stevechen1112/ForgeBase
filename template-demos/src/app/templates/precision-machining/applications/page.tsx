import type { Metadata } from "next";
import { ApplicationsPage } from "@/templates/precision-machining/components/PrecisionSitePages";

export const metadata: Metadata = {
  title: "Industry Applications | AxisForm Demo",
  description: "See how a precision-machining site can organize content around automation, instrumentation and mobility sourcing contexts.",
};

export default ApplicationsPage;
