import type { Metadata } from "next";
import { RFQPage } from "@/templates/precision-machining/components/PrecisionSitePages";

export const metadata: Metadata = {
  title: "Send a Drawing | AxisForm Demo",
  description: "Try a safe static RFQ interaction that does not upload, transmit, save or email information.",
};

export default RFQPage;
